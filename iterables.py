# Iterables = An Object/Collection that can return elements one at a time allowing to be iterated over in a loop

numbers = [1, 2, 3, 4, 5]

# for number in numbers:
#     print(number)
for number in reversed(numbers):
    print(number)
